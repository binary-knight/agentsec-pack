{
  "_meta": {
    "description": "Measurements of sandboxes shipped by named agents. These are RECORDED RESULTS, not runnable presets: an agent's own sandbox is entered by the agent, so the probe is run inside it via the agent itself and scored with `agentsec score`. Every entry states the exact version, date, host and command.",
    "rules": "No entry may be added from documentation or recall. The probe must have been run inside the sandbox and its JSON kept. A vendor's documented design boundary is recorded as such, not as a vulnerability."
  },
  "profiles": {
    "codex-cli-0.153.4-read-only": {
      "agent": "OpenAI Codex CLI",
      "controls": "unix_control: bind-and-connect to a socket the probe creates itself. unix_control_nopath: connect to a nonexistent path, which needs no writable directory and separates a blocked call (PermissionError) from a missing file (FileNotFoundError).",
      "host": "Ubuntu 24.04, kernel 6.8, bubblewrap 0.9.0 on PATH",
      "how": "codex exec -m gpt-6-astra -c model_reasoning_effort=low -c service_tier=default --sandbox read-only --skip-git-repo-check, asked to run the probe and reply with its complete stdout. Read-only mode cannot write a file, so the JSON is recovered from the transcript; ask for the whole object, because a tail-limited reply truncates the leading keys and the result will not parse.",
      "measured": "2026-09-08",
      "mode": "--sandbox read-only",
      "model": "gpt-6-astra (model_reasoning_effort=low, service_tier=default)",
      "report": "examples/agents/codex-cli-0.153.4-read-only.md",
      "result": "Contained on the write and network axes: root filesystem read-only, working directory not writable, DNS resolution fails and direct-IP connects are refused, container sockets visible but every connection refused. A control test confirms the refusal is not path-scoped: connecting to a path that does not exist returns PermissionError rather than FileNotFoundError, so unix-domain connect is blocked before the path is consulted and the visible socket paths are inert. Residual exposure is READ access to the invoking user's home directory.",
      "score": 41,
      "vendor_docs": "https://learn.chatgpt.com/codex/sandboxing (fetched 2026-09-08; the page carries no version or revision date, so it describes the vendor's documentation as of that read and not a pinned release): \"The Linux sandbox relies on bubblewrap for enforcement\"; read-only mode: \"The agent can inspect files, but it can't edit files or run commands without approval.\"",
      "version": "0.153.4"
    },
    "codex-cli-0.153.4-workspace-write": {
      "agent": "OpenAI Codex CLI",
      "controls": "unix_control: bind-and-connect to a socket the probe creates itself. unix_control_nopath: connect to a nonexistent path, which needs no writable directory and separates a blocked call (PermissionError) from a missing file (FileNotFoundError).",
      "host": "Ubuntu 24.04, kernel 6.8, bubblewrap 0.9.0 on PATH",
      "how": "codex exec -m gpt-6-astra -c model_reasoning_effort=low -c service_tier=default --sandbox workspace-write --skip-git-repo-check, asked to run the probe redirecting stdout to a file inside the workspace.",
      "measured": "2026-09-08",
      "mode": "--sandbox workspace-write",
      "model": "gpt-6-astra (model_reasoning_effort=low, service_tier=default)",
      "report": "examples/agents/codex-cli-0.153.4-workspace-write.md",
      "result": "As read-only, plus a writable working directory (expected, scored informational) and read-write bind mounts from the host. The unix-socket control runs here and fails at bind with PermissionError, and the no-path control returns PermissionError, so unix-domain socket operations are refused wholesale in this mode too.",
      "score": 49,
      "vendor_docs": "https://learn.chatgpt.com/codex/sandboxing (fetched 2026-09-08; the page carries no version or revision date, so it describes the vendor's documentation as of that read and not a pinned release): workspace-write mode: \"The agent can read files, edit within the workspace, and run routine local commands inside that boundary.\"",
      "version": "0.153.4"
    }
  }
}
